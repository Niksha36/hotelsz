<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="true"
    cacheDirectory="var/cache/.psalm-cache"
    ignoreInternalFunctionFalseReturn="false"
    ignoreInternalFunctionNullReturn="false"
    ensureArrayIntOffsetsExist="true"
    ensureArrayStringOffsetsExist="true"
    strictBinaryOperands="true"
>

    <!-- какие файлы анализируем -->
    <projectFiles>
        <directory name="src" />

        <!-- исключаем отдельные входные файлы и т.п. -->
        <ignoreFiles>
            <directory name="bin" />
            <directory name="config" />
            <directory name="vendor" />
            <file name="tests/bootstrap.php" />
            <directory name="var" />
            <directory name="public"/>
        </ignoreFiles>
    </projectFiles>

    <!-- подключаем плагин Symfony (если установлен) -->
    <plugins>
        <pluginClass class="Psalm\SymfonyPsalmPlugin\Plugin">
            <!-- путь для Symfony 5+ -->
            <containerXml>var/cache/dev/App_KernelDevDebugContainer.xml</containerXml>
            <!-- если есть тестовый контейнер -->
            <containerXml>var/cache/test/App_KernelTestDebugContainer.xml</containerXml>
            <!-- если используете PHP-конфиги (Symfony 5.3+): -->
            <!-- <extraFiles><directory name="var/cache/dev/Symfony/Config" /></extraFiles> -->
        </pluginClass>
    </plugins>

    <issueHandlers>
        <PossiblyUnusedMethod>
            <errorLevel type="suppress">
                <file name="src/Entity/*" />
            </errorLevel>
        </PossiblyUnusedMethod>
    </issueHandlers>

</psalm>
